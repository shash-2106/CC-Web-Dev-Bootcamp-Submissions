# Simple To-Do App

- Minimal web-based to-do list with profile section  
- Add, delete, and clear tasks dynamically  
- Input validation prevents empty entries  
- Checkbox + delete button per task  
- Clear button auto-disables when list is empty  
- Clean UI with centered layout and card styling  
- Built using plain HTML, CSS, and JavaScript  
- No backend, runs fully in browser 